SELECT 'create_schema.sql: Starting execution...' AS LogMessage;

SELECT 'Using UserManagementDB database as schema.' AS LogMessage;
USE UserManagementDB;

SELECT 'create_schema.sql: Execution finished successfully.' AS LogMessage;
